# Copy to infra/vultr/.env (gitignored) before `docker compose up`.
# deploy.ps1 does not ship this file to the server; set it there too.

# Required — the ats service refuses to start without it.
ATS_WORKER_TOKEN=

# Leave false unless you intend this box to be able to send real applications.
ATS_ALLOW_SUBMIT=false
